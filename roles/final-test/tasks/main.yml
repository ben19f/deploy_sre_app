- name: Save previous version
  command: readlink /opt/sre-app
  register: previous_release
  changed_when: false

- name: Wait PostgreSQL
  command: pg_isready -U {{ postgres_user }}
  register: pg_check
  retries: 20
  delay: 3
  until: pg_check.rc == 0


