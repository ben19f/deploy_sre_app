sudo apt install git

установка докера


git clone https://github.com/muxx/sre-hello-world
cd sre-hello-world

создаю свой docker-compose
version: "3.1"

services:
    
    php:
        build: docker/images/php
        working_dir: /var/www/sre
        volumes:
            - ./:/var/www/sre
		ports:  
			- "127.0.0.1:9000:9000"
        environment:
            - POSTGRES_HOST=127.0.0.1
            - POSTGRES_DB
            - POSTGRES_USER
            - POSTGRES_PASSWORD



docker-compose up -d


ip route | grep docker  
172.17.0.0/16 dev docker0 proto kernel scope link src 172.17.0.1 linkdown